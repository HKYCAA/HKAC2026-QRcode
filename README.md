# HKAC 2026 QR Check-in

A mobile-first QR attendance system for the HKAC 2026 live event. The frontend
runs on GitHub Pages and sends scanned or manually entered codes to a Google
Apps Script web app.

## How it works

1. `html5-qrcode` starts with the rear camera and scans a QR code in the
   attendee's phone browser.
2. The frontend extracts the leading unique code and places it in the input.
3. The operator reviews the code and presses **Check in**.
4. The frontend posts the unique code to the Apps Script `doPost` endpoint.
5. Apps Script finds the code in `all!Code` in **HKAC 2026 full list**.
6. A successful check-in is appended to the `qrcode-scan` tab.
7. A script lock makes simultaneous scans safe and prevents duplicate rows.

The source `Code` column is read-only. The backend never writes to it.

## Project structure

- `index.html`, `styles.css`, `app.js` — GitHub Pages scanner interface.
- `core.js` — frontend code normalization and result messages.
- `config.js` — deployed Apps Script web app URL.
- `QRCheckin.js` — `doPost` check-in backend.
- `Code.js`, `ecert.js` — archived utilities from the original bound project;
  `.claspignore` excludes them from the check-in deployment.
- `appsscript.json` — Apps Script manifest and public web-app configuration.
- `.clasp.json` — links this checkout to the dedicated standalone
  `qrcode-scan` Apps Script project.
- `tests/` — dependency-free frontend and backend unit tests.

## Live sheet schema

Source tab: `all`

- `Code` — unique QR value; read-only and generated by the spreadsheet.
- `Chi+Eng` — preferred display name.

Attendance tab: `qrcode-scan`

| Timestamp | Code | Name | Source Row |
| --- | --- | --- | --- |
| Apps Script date | Unique code | Combined name | Matching row in `all` |

## Local checks

```sh
npm test
npm run check
```

## Apps Script deployment

1. Install and authenticate `clasp`:

   ```sh
   npm install --global @google/clasp
   clasp login
   clasp show-authorized-user
   ```

2. Review the files that will be uploaded:

   ```sh
   clasp status
   ```

3. Push the Apps Script source:

   ```sh
   clasp push
   ```

4. Create the public web-app deployment:

   ```sh
   clasp create-deployment --description "HKAC 2026 QR check-in"
   ```

5. In the Apps Script editor, choose `authorizeCheckin` in the function menu
   and click **Run** once. Review and approve the Google Sheets permission for
   the deployment owner. Standalone Apps Script projects request account-wide
   Sheets access; Google does not offer a per-spreadsheet scope here.

6. The production URL has this form:

   ```text
   https://script.google.com/macros/s/DEPLOYMENT_ID/exec
   ```

7. Put that URL in `config.js`, rerun the checks, then commit and push.

Current production endpoint:

```text
https://script.google.com/macros/s/AKfycbwLMXogTicd0GZK6tu0X8VxHIu-7_B7i8cKlC0FvHFvouslFSABxQOAo1nZqK2woSrc/exec
```

If an existing deployment is being updated, use:

```sh
clasp create-deployment \
  --deploymentId DEPLOYMENT_ID \
  --description "HKAC 2026 QR check-in update"
```

## GitHub Pages deployment

The site is a static application and needs no build step.

Live site: <https://hkycaa.github.io/HKAC2026-QRcode/>

1. Push `main` to GitHub.
2. In the repository, open **Settings → Pages**.
3. Under **Build and deployment**, choose **Deploy from a branch**.
4. Select `main` and `/ (root)`, then save.
5. Open the published HTTPS URL on a phone and allow camera access.

## Event-day test checklist

- Scan a QR containing a code and name: only the leading code appears in the
  input and no attendance is recorded yet.
- Scan another QR before submitting: the new code replaces the previous code.
- Press **Check in** for one known valid code: success shows the code first,
  followed by name.
- Scan the same code again: **Already checked in**.
- Enter a fake code manually: **Invalid code**, with no log row added.
- Test camera permission and manual fallback on both iPhone and Android.
- Keep the Apps Script deployment URL private from casual editing, although the
  endpoint itself must be publicly callable for check-in.

Always review the diff before `clasp push`: it updates the bound Apps Script
project.
